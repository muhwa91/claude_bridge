# claude_bridge 런타임 의존성.
# 코어(bridge.py)·계약(adapter.py)은 표준 라이브러리 전용(의존성 0).
# 디스코드 어댑터(discord_adapter.py)만 discord.py 를 쓰며 bridge.main 이 지연 import 하지만,
# 디스코드 전용이라 구동에 필수다(미설치 시 기동 불가).
#
# 설치:
#   python -m pip install -r requirements.txt
discord.py==2.7.1
# '/노래' 음성 재생(디스코드 음성 어댑터 전용). PyNaCl=음성 암호화, imageio-ffmpeg=번들 ffmpeg,
# yt-dlp=재생목록·스트림 추출(유튜브 대응 잦은 갱신 → 상한 핀 없이 플로어만), davey=DAVE 음성
# 프로토콜(discord.py 2.7 필수 — 없으면 'voice will NOT be supported' 로 음성 미동작).
PyNaCl==1.5.0
imageio-ffmpeg==0.6.0
yt-dlp>=2026.7.4
davey==0.1.6
